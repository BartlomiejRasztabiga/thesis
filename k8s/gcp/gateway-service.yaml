apiVersion: v1
kind: Service
metadata:
  name: gateway
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: NodePort
  ports:
    - name: "8100"
      port: 8100
      targetPort: 8100
  selector:
    app: gateway
